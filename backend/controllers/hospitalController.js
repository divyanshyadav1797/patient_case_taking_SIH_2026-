const getHospitalData = async (req, res) => {
  // TODO: return hospital profile, staff counts, and operations snapshot
  res.status(501).json({ message: 'Get hospital data not implemented yet' });
};

module.exports = { getHospitalData };
