# MediKiosk frontend

React + Vite app for patient, doctor, hospital, and kiosk portals.

```bash
npm install
npm run dev
```

Dev server: http://localhost:5173  
API proxy: `/api` → http://localhost:5000
