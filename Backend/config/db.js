// TODO: Connect to MongoDB
// const mongoose = require('mongoose');

const connectDB = async () => {
  // mongoose.connect(process.env.MONGO_URI)
  console.log('DB connection placeholder - implement with MongoDB');
};

module.exports = connectDB;
